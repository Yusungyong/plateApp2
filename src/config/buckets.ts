import Config from 'react-native-config';

export const VIDEO_BUCKET = (Config as any).VIDEO_BUCKET || '';
export const IMAGE_BUCKET = (Config as any).IMAGE_BUCKET || '';
export const IMAGE_BUCKET_THUMBNAIL = (Config as any).IMAGE_BUCKET_THUMBNAIL || '';
export const FEED_IMAGE_BUCKET = (Config as any).FEED_IMAGE_BUCKET || '';
